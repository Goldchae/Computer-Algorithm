# 기본 퀵 정렬 의사 코드

함수 기본퀵정렬(배열)
    만약 배열의 길이가 1 이하면
        반환 배열
    아니면
        피벗 <- 배열의 첫 번째 원소
        더 작은 값들 <- 피벗보다 작은 배열의 원소들
        같은 값들 <- 피벗과 같은 배열의 원소들
        더 큰 값들 <- 피벗보다 큰 배열의 원소들
        반환 기본퀵정렬(더 작은 값들) + 같은 값들 + 기본퀵정렬(더 큰 값들)
끝
