절차 findMaxMinType1(A, n)
    min = A[0]
    max = A[0]
    for i = 1 to n-1 do
        if A[i] > max then
            max = A[i]
        else if A[i] < min then
            min = A[i]
    return (min, max)
